@inherits LayoutComponentBase
@inject NavigationManager NavManager

<MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="_theme"/>
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Color="Color.Tertiary" Elevation="0">
        <MudToggleIconButton @bind-Toggled="@_isDarkMode"
                             Icon="@Icons.Material.Filled.DarkMode" Color="Color.Inherit"
                             ToggledIcon="@Icons.Material.Filled.LightMode" ToggledColor="Color.Inherit" />
        <MudIconButton Icon="@Icons.Material.Filled.Construction" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => pca())" />
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.DirectionsRailway" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => Home())" />
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.Person" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => pc())" />
        <MudIconButton Icon="@Icons.Material.Filled.ShoppingCart" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => b())" />
    </MudAppBar>
@*    <MudDrawer @bind-Open="_drawerOpen" Elevation="1">
        <MudDrawerHeader>
            <MudText Typo="Typo.h6">MudBlazorTemplates1</MudText>
        </MudDrawerHeader>*@
@*        <NavMenu />
    </MudDrawer>
        <MudDrawer @bind-Open="_drawerOpen" Elevation="1">
        <MudDrawerHeader>
            <MudText Typo="Typo.h6">MudBlazorTemplates1</MudText>
        </MudDrawerHeader>
        <NavMenu />*@
    @*</MudDrawer>*@ 
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.Large" Class="my-16 pt-16">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>



@*@code {
    bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
}*@


@code {

    void Home()
    {
        NavManager.NavigateTo("/");
    }

    void pc()
    {
        NavManager.NavigateTo("/login");
    }

    void b()
    {
        NavManager.NavigateTo("/basket");
    }

    void pca()
    {
        NavManager.NavigateTo("/login_adm");
    }

    private MudTheme _theme = new();
    private bool _isDarkMode;
    public bool AlarmOn { get; set; }
}